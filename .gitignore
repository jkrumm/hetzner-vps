# TLS certificates — never commit
traefik/acme.json

# Local env overrides / materialized secrets — never commit.
# Broad match with an explicit allow for the op:// templates, which are safe.
.env
.env.*
*.env
!*.env.tpl
!.env.tpl

# OS
.DS_Store

# Editor
.idea/
.vscode/

# Doppler local config
.doppler.yaml
